# Sample Express MongoDB Project

## Как запустить проект

1. Установите зависимости:
```bash
docker-compose run app npm install --include=dev
```

2. Запустите приложение и MongoDB:
```bash
docker-compose up -d
```
